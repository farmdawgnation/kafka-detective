version: "2"

services:
  kafka:
    image: "spotify/kafka"
    ports:
      - "127.0.0.1:2181:2181"
      - "127.0.0.1:9092:9092"
    environment:
      ADVERTISED_PORT: 9092
      ADVERTISED_HOST: kafka
      ZK_CHROOT: "kafka"
